import React from 'react';
import './logo.styles.scss';

const Logo = ( { userName } ) => {
    return (
        <div className="row logo-holder">
            <div className={`logo-img`} />
        </div>
    )
};

export default Logo;
